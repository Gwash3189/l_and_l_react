import React, { Component } from 'react';

export default class Category extends Component {
  static defaultProps = {
    category: {}
  };

  render() {
    return (
      <div>
        Category
      </div>
    );
  }
}
