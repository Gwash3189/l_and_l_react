import React, { Component } from 'react';

export default class CategoryContainer extends Component {
  static defaultProps = {
    category: {}
  };

  render() {
    return (
      <div>
        Category Container
      </div>
    );
  }
}
